# vega-reproducer

A Vega-operated builder that independently rebuilds attested Nix outputs from their provenance and attests the result, feeding the `vega-repro` lane of the Vega binary cache. See https://docs.vega-cache.dev/reproducibility.